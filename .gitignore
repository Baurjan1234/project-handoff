node_modules/
dist/
build/
.next/
out/
coverage/
new-expert-ui-export/
*.tsbuildinfo

# Secrets — hard rule 2. Never commit keys, seeds, or operator IDs.
.env
.env.*
!.env.example

# Per-developer Claude Code overrides
.claude/settings.local.json

.DS_Store
*.log

# Playwright MCP scratch output: page snapshots, console logs, screenshots.
# Written into the working directory by the browser tools, never part of a change.
.playwright-mcp/
